[package]
name = "traffic"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
bytemuck = { version = "1.25", features = ["derive"] }
engine = { path = "../../crates/engine" }
log = "0.4"
