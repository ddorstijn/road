[package]
name = "shaders"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
spirv-std = "0.10.0-alpha.1"
gpu-shared = { path = "../gpu-shared", default-features = false, features = [] }
